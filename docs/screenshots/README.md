# Screenshots

Drop three PNGs here, named exactly as below — the root README already references them.

| Filename | What to capture | Why it earns its place |
|---|---|---|
| `verdict-real-discount.png` | The verdict for **"Is the Compare at Price Snowboard worth it?"** — showing the confirmed 11% markdown, and the advice to rent instead | The single most persuasive image in the repo: live merchant data used to argue *against* the sale |
| `verdict-out-of-stock.png` | The verdict for **"Should I buy the Out of Stock Snowboard?"** | Shows the tool leading with what saves the user time |
| `landing.png` | The landing page | Sets the tone before anyone reads a line of code |

Capture at a phone-ish width (around 500–600px) so the cards fill the frame rather than
floating in whitespace. On macOS, `Cmd+Shift+4` then drag.
